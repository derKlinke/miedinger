{
    "name": "@derklinke/miedinger",
    "version": "0.9.4",
    "description": "Shared formatter configs",
    "repository": {
        "type": "git",
        "url": "https://github.com/derKlinke/miedinger.git"
    },
    "license": "MIT",
    "bin": {
        "miedinger": "dist/format-configs.js"
    },
    "scripts": {
        "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs');const p='dist/format-configs.js';const data=fs.readFileSync(p,'utf8');if(!data.startsWith('#!')) fs.writeFileSync(p,'#!/usr/bin/env node\\n'+data);fs.chmodSync(p,0o755);\"",
        "format": "npx --yes prettier --config configs/.prettierrc.json --ignore-path configs/.prettierignore --write . && npx --yes -p markdownlint-cli markdownlint --config configs/.markdownlint.json --ignore-path configs/.markdownlintignore \"**/*.md\"",
        "format:check": "npm run format && git diff --exit-code"
    },
    "devDependencies": {
        "@semantic-release/changelog": "^6.0.3",
        "@semantic-release/commit-analyzer": "^13.0.1",
        "@semantic-release/exec": "^7.1.0",
        "@semantic-release/git": "^10.0.1",
        "@semantic-release/github": "^12.0.2",
        "@semantic-release/release-notes-generator": "^14.1.0",
        "@types/node": "22.13.10",
        "semantic-release": "^25.0.2",
        "typescript": "5.8.2"
    },
    "files": [
        "dist",
        "configs",
        "README.md",
        "LICENSE"
    ],
    "publishConfig": {
        "access": "public"
    },
    "dependencies": {
        "yaml": "^2.8.2"
    }
}
